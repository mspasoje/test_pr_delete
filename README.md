# test_pr_delete
better?
maybe now...?
what??
                 nbWritten = Encoding.UTF32.GetBytes(s, 0, s.Length, pbMem, nb);
                 
????
 nbWritten = Encoding.UTF32.GetBytes(s, 0, s.Length, pbMem, nb);
      nbWritten = Encoding.UTF32.GetBytes(s, 0, s.Length, pbMem, nb);
